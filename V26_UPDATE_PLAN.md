# v26 update channel

This version adds an explicit in-app update check, visible installed version on the home screen, reliable candidate-drawer collapse/restore behavior, and keeps candidate notes visible across cell selection changes.

The game remains offline during play. Network access is used only after the player explicitly taps “Проверить обновление”.
